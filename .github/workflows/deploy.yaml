name: "Deploy Streams 🚀🚀"

on: 
    workflow_dispatch:
    push:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: "Checkout repository ⚒️"
              uses: actions/checkout@v4
            - name: "⚙️ Setup .NET 8"
              uses: actions/setup-dotnet@v4
              with:
                dotnet-version: 8.x
            - name: "Restore dependencies 🗳️"
              run: dotnet restore ./StreamsMS.csproj
            - name: "Build project 🏗️👷‍♀️"
              run: dotnet build ./StreamsMS.csproj --no-restore
            - name: "Run Tests ✅✅"
              run: dotnet test ./StreamsMS.csproj --no-build --verbosity normal